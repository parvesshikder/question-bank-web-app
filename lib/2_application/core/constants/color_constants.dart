import 'package:flutter/material.dart';

const primaryColor = Color(0xFF685BFF);
const canvasColor = Color(0xFF2E2E48);
const scaffoldBackgroundColor = Color(0xFF464667);
const accentCanvasColor = Color(0xFF3E3E61);
const white = Colors.white;
final actionColor = const Color(0xFF5F5FA7).withOpacity(0.6);
final divider = Divider(color: white.withOpacity(0.3), height: 1);
const backgroundGradient = LinearGradient(
  colors: [Color(0xffffffff), Color(0xffc5cbeb)],
  stops: [0, 1],
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
);
const blackText = Color(0xFF1F1F1F);
const greyext = Color(0xFF6E8495);
const color1Gradient = LinearGradient(
  colors: [Color(0xff6ab9f7), Color(0xff0a6cba)],
  stops: [0, 1],
  begin: Alignment.centerRight,
  end: Alignment.centerLeft,
);
const color2Gradient = LinearGradient(
  colors: [Color(0xffffeb3b), Color(0xffe91e63)],
  stops: [0, 1],
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
);
